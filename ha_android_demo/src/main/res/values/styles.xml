<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/monitor_main</item>
        <item name="colorPrimaryDark">@color/monitor_main</item>
        <item name="colorAccent">@color/monitor_main</item>
        <item name="colorOnSecondary">@color/white</item>
    </style>

    <style name="myNewTheme">
        <item name="android:background">@color/color_bg_one</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:layout_marginTop">16dp</item>
    </style>

    <style name="RoundedBottomSheetDialog" parent="Theme.Design.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/TransparentBottomSheetStyle</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>

    <style name="TransparentBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>
